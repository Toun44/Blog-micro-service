server:
  port: 8083

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/comment_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

feign:
  circuitbreaker:
    enabled: true
